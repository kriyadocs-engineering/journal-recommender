# Database connection
# For local development with docker-compose.dev.yml:
DATABASE_URL=postgresql://journal_user:journal_pass_2024@localhost:5433/journal_recommender

# For production (Railway/Render/Supabase will provide this):
# DATABASE_URL=postgresql://user:password@host:5432/database

# Server Port
PORT=3001

# Node Environment (development | production)
NODE_ENV=development

# OpenAI API Key (OPTIONAL - not currently used)
# The app uses keyword-based matching, not LLM
# Uncomment if you add LLM-powered recommendations in the future
# OPENAI_API_KEY=sk-your-openai-key-here
